# AGENTS.md — Emora 运营后台开发规范

本文件适用于 `D:\work\emora-admin` 整个仓库。后续修改本项目时，必须先阅读本文件，再阅读与任务相关的源码和知识库。用户在当前会话中的明确要求优先于本文件。

## 1. 项目定位

- 本仓库是 Emora 运营后台前端，不是 PHP 后端项目。
- 技术栈：React 19、React Router、Vite 6、Ant Design 6、Day.js。
- 前端源码目录：`D:\work\emora-admin`。
- 后端仓库：`D:\work\emora-api`。需要修改接口时必须切换到后端仓库，并阅读后端仓库自己的 `AGENTS.md`。
- 生产静态制品归档仓库：`D:\work\emora.build`。
- 管理后台生产制品目标目录：`D:\work\emora.build\emora-admin`。
- 管理后台开发制品目标目录：`D:\work\emora.build\emora-admin.dev`。
- 不得把源码仓库里的普通 `dist` 目录当成最终生产交付目录。

## 2. 开始修改前

先完整检查实际调用链，不要只看截图直接改 CSS：

```text
浏览器路径
→ src/admin/Admin.jsx 中的 NAV_GROUPS 与页面选择
→ 对应页面组件
→ src/admin/api/client.js
→ src/config/environments.json
→ knowledge-base 中对应接口文档
→ 后端接口（仅在任务确实涉及后端时）
```

至少完成以下检查：

1. 阅读 `package.json`、`vite.config.mjs` 和 `knowledge-base/README.md`。
2. 找到页面状态所有者、事件处理函数、接口调用和相关 CSS。
3. 检查同一组件在白色模式和黑色模式下的表现。
4. 检查加载、空数据、失败、禁用、重复点击和长文本场景。
5. 改接口契约前，阅读对应的 `knowledge-base/api/*.md` 和真实调用方。

## 3. 接口与数据约束

- 所有后台请求统一通过 `src/admin/api/client.js` 调用。
- 后台业务接口只允许使用 `/admin/*`。
- 登录流程仅例外使用 `/auth/email/code` 和 `/auth/email/login`。
- 不接入根路径 `/ai/*` 的 C 端聊天接口。
- 开发环境 API 基址来自 `src/config/environments.json` 的 `dev` 配置。
- 生产环境 API 基址来自 `src/config/environments.json` 的 `prod` 配置。
- 保持现有 URL、HTTP 方法、请求参数、响应字段、状态值和时间单位兼容。
- 页面需要新字段时，先确认接口真实返回；不得编造统计值、状态或假数据。
- 接口失败、404 或未认证时，使用明确的 Ant 错误或空状态，不回退到原型 mock 数据。
- 不在日志、页面、提交或文档中暴露 Token、API Key、支付凭证和第三方原始回调数据。
- 列表请求必须保留分页；禁止为了前端筛选一次加载无限量数据。
- 可取消的请求使用 `AbortController`，组件卸载或条件变化时取消旧请求。
- 用户详情等重数据按需加载，不得在列表循环中逐条请求。

## 4. Ant Design 6 组件规范

页面控件优先使用 Ant Design 6 官方组件。禁止继续用原生 HTML 加自定义 CSS 仿制已有的 Ant 组件。

统一对应关系：

- 按钮：`Button`
- 输入框：`Input`、`InputNumber`、`Input.TextArea`
- 下拉选择：`Select`
- 开关：`Switch`
- 表格：`Table`
- 分页：`Pagination`
- 页签：`Tabs`
- 二选一或少量互斥选项：`Segmented`
- 弹窗与确认：`Modal`
- 抽屉：`Drawer`
- 提示消息：`App.useApp().message`
- 页面错误：`Alert`
- 加载：`Spin`
- 空状态：`Empty`
- 状态标签：`Tag`
- 日期：`DatePicker`
- 上传：`Upload`
- 面包屑：`Breadcrumb`
- 可复制文本：`Typography.Text` 的 `copyable`，或带官方图标的 `Button`

禁止使用：

- `window.alert`、`window.confirm`、`window.prompt`
- 自制 Toast、Loading、Switch、Pagination、Select、Modal
- 全局覆盖 `.ant-btn`、`.ant-input` 等组件的字体、内边距、高度、圆角和颜色
- 为了匹配旧页面而给 Ant 组件重新实现一套视觉系统

## 5. 按钮与表单规则

- 普通业务按钮使用 Ant 默认 `middle` 尺寸，不传 `size="small"`。
- `UiButton` 中旧的 `sm` 类只作为兼容输入被移除，不得再次映射为小号尺寸。
- 主操作使用 `type="primary"`；删除等危险操作使用 `danger`。
- 表格操作按钮也保持官方按钮字体和内边距，空间不足时让表格横向滚动或合理换行。
- 输入框和按钮组合使用 `Space.Compact`，保证高度、边框和焦点状态一致。
- 标签输入区域宽度保持紧凑，当前约定最大约 420px；支持回车添加，并处理中文输入法的组合输入状态。
- 搜索框使用 Ant 默认圆角和配色，不添加过大的胶囊圆角。
- 长文本使用 `Input.TextArea autoSize`，根据内容伸缩，并设置合理的最小、最大行数。
- 表单提交必须包含加载或禁用状态，避免重复提交。

## 6. 导航与主题规则

- 左侧导航使用 `Layout.Sider + Menu`。
- 侧栏宽度固定为 232px，不把整个侧栏折叠成窄图标栏。
- 导航分类使用 Ant `Menu` 子菜单：点击分类标题通过右侧箭头展开或收起子项。
- 当前分类为：概览、内容与 AI、运营管理、商业化、系统管理。
- 各分类展开状态保存在浏览器本地；不得改变页面 URL 或菜单业务 ID。
- 页面级导航使用 `Tabs` 的官方下划线选中样式。
- `Segmented` 只用于平台、商品类型、资产类型等明确的互斥切换，不替代页面级导航。
- 白色模式使用 `theme.defaultAlgorithm`，黑色模式使用 `theme.darkAlgorithm`。
- 不覆盖 Ant 默认主题 token 来模拟另一套配色。
- 新增组件必须同时检查白色和黑色模式。

## 7. 表格、分页和展示规则

- 列表统一使用 Ant `Table`，分页统一使用 Ant `Pagination`。
- 分页提供总数、上一页、下一页、页码和每页数量选择，选项统一为 10、20、50、100。
- 列宽根据字段内容相对自适应。短 ID、状态和操作列收缩；邮箱、订单号、描述等长字段获得更多空间。
- 必须处理窄屏或长字段，使用表格横向滚动、截断提示或受控换行，不能让页面布局被撑坏。
- 所有 ID 展示默认不添加 `#` 前缀。
- 用户 ID、`user_uuid`、系统订单号和第三方订单号等常用标识按需求提供复制操作。
- 复制按钮和对应文本保持同一行，点击复制不得触发表格行的详情事件。
- 空值统一显示 `—`；无数据使用 `Empty`。
- 状态使用 Ant `Tag`，不要使用自定义圆角徽章仿制。
- 角色描述、标签等列表内容应限制展示高度或长度，完整内容放到详情页或详情弹窗。

## 8. 当前页面交互约定

- 角色编辑使用页面内编辑器；主要区域通过 `Tabs` 切换。
- 人设、对话规则和扩展 JSON 等长文本输入根据内容自动伸缩。
- 中转站配置不使用大弹窗；从列表跳转到独立编辑页面。
- 中转站文本、图片、视频模型分列展示；新增模型追加在同类型上一模型下方。
- 每个模型使用清楚的虚线边界区分；新模型不得替换当前已启用的默认模型。
- 用户列表操作为“查看详情”，完整资料按需加载到 Ant 弹窗。
- 订单和订阅列表保持当前字段顺序、平台筛选与复制交互，不随意恢复已删除的冗余字段。
- 系统设置接口不存在或未部署时明确显示真实 404，不伪造本地保存成功。

## 9. CSS 规则

- 布局类 CSS 可以保留，例如网格、间距、最大宽度、滚动区域和响应式断点。
- 不修改 Ant 官方组件内部字体、行高、内边距、控件高度和主题颜色。
- 不使用高优先级选择器或 `!important` 强行覆盖 Ant，除非修复第三方明确缺陷并写中文注释说明原因。
- 新增样式前先搜索是否已有同用途类名，避免重复规则。
- 删除组件时同步删除失效 CSS。
- 白色模式正文颜色必须保持 Ant 默认清晰度，不使用降低可读性的透明度或字体平滑技巧。
- 页面外框、嵌套卡片和重复边框只在确实表达信息层级时使用，避免无意义的多层框。

## 10. 代码规范

- 优先复用现有组件和 API 封装，不做与需求无关的大范围重构。
- 复杂新页面可以拆到独立 `*.jsx` 和 `*.css`，不要继续无边界扩大单文件。
- 使用清楚的变量名和函数名；布尔值优先使用 `is`、`has`、`can`、`should` 开头。
- 新增核心逻辑添加有价值的中文注释，说明兼容、状态、失败处理或设计原因。
- 不写伪代码、占位实现或“其余代码不变”。
- 不静默吞掉异常；用户可恢复的错误必须有可读提示。
- 不把密钥、Token、真实账号或生产凭据写入仓库。
- 不无理由新增依赖；确需新增时先确认现有 Ant、React 或浏览器能力不能满足。

## 11. 验证要求

每次修改至少执行：

```powershell
npm run validate
git diff --check
```

`npm run validate` 会执行知识库/API 客户端一致性检查和 Vite 生产构建。不得硬编码文档接口数量，以命令实际输出为准。

涉及界面时还要：

1. 使用 `npm run dev` 启动本地页面，默认端口以 `vite.config.mjs` 为准。
2. 核对目标页面的正常、加载、空数据和失败状态。
3. 核对白色模式和黑色模式。
4. 核对长文本、滚动、分页和交互控件。
5. 只报告真实执行过的检查；没有真实账号或后端环境时，明确说明未完成真实联调。

构建产生大分包警告时如实报告。除非任务就是性能优化，不要为了消除警告擅自重构业务模块。

## 12. 生产打包

用户要求“打包生产版本”时，最终目标是 `D:\work\emora.build\emora-admin`，不是源码仓库中的 `dist`。

推荐流程：

1. 在源码仓库执行 `npm run validate`。
2. 使用 `npm run build -- --outDir dist-prod` 生成独立生产暂存目录。
3. 确认 `dist-prod/index.html` 和静态资源存在。
4. 在 PowerShell 中解析并核对目标绝对路径确实位于 `D:\work\emora.build` 下。
5. 将生产文件同步到 `D:\work\emora.build\emora-admin`，不得删除或覆盖聚合仓库的 `.git`。
6. 比较入口文件和关键静态资源，确认复制结果与本次构建一致。
7. 分别报告源码构建、制品同步、Git 提交、远程推送和服务器部署状态；任何一步未执行都必须明确说明。

开发版本使用 `npm run build:dev`，目标为 `D:\work\emora.build\emora-admin.dev`，不得和生产目录混用。

## 13. Git 与部署边界

- 修改前检查当前分支和工作区状态，不覆盖用户已有未提交改动。
- 创建新分支时遵循用户指定名称；没有指定时使用 `codex/` 前缀。
- 只有用户明确要求时才提交、推送、合并或部署。
- 提交前检查 `git diff`、`git diff --check` 和验证结果。
- 源码仓库与 `D:\work\emora.build` 聚合仓库是两个独立 Git 边界，必须分别检查分支、远程和提交。
- “构建成功”不等于“制品已同步”，“推送成功”不等于“服务器已部署”。
- 推送后核对本地提交与远程目标分支；部署后核对实际入口资源或哈希。
- 未经用户明确授权，不执行远程 SQL、数据库迁移或生产服务器修改。

## 14. 完成交付格式

最终回复至少说明：

- 分析结果和问题原因
- 修改方案与兼容性
- 修改文件
- 数据库变化（前端任务通常为“无”）
- 配置变化
- 实际执行的验证
- 打包、提交、推送、部署分别是否完成
- 已知风险和未完成的真实环境联调

所有结论必须以实际代码和实际检查为依据。
